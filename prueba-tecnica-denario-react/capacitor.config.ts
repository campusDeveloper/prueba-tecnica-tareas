import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba-tecnica-denario-react',
  webDir: 'dist'
};

export default config;
